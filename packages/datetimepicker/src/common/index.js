// FIXME: This is a stub for now to try to prevent crashes from importing/rendering
// Should probably standardise on Android/Material Design date picker first for this primitive
// TODO: Split this up into multiple re-usable components
// This should be injectable too, to override the web implementation
const { View: DateTimePicker } = require('react-primitives');

module.exports = DateTimePicker;
